# Story 1.1: Clone and setup MiniRAG in src/MiniRAG

## Status
Ready for Review

## Story
**As a** Developer,
**I want** to clone and setup the MiniRAG library within the project,
**so that** we can use it as the core engine for our new memory system.

## Acceptance Criteria
1. Clone MiniRAG repo into `src/MiniRAG`.
2. Ensure the directory structure places MiniRAG at `src/MiniRAG` or appropriate root so it can be imported.
3. Verify that `MiniRAG` dependencies are compatible or installable.

## Tasks / Subtasks
- [x] Task 1: Clone MiniRAG Repository (AC: 1, 2)
    - [x] Execute `git clone https://github.com/HKUDS/MiniRAG src/MiniRAG`
    - [x] Verify the cloned directory structure matches expectations (e.g., `src/MiniRAG` contains the source code).
    - [x] Remove `.git` folder from `src/MiniRAG` if we are treating it as vendored code within our monorepo (or keep it if submodule strategy is preferred - Architecture says "Submodule/Source in src/MiniRAG", treating as Source for now is safer for modification). *Decision: Keep it simple, just clone.*
- [x] Task 2: Manage Dependencies (AC: 3)
    - [x] Inspect `src/MiniRAG/requirements.txt`.
    - [x] Install necessary dependencies using `uv add` or `uv pip install` to valid compatibility.
    - [x] Ensure no conflicts with existing project dependencies (especially `litellm` or `ollama`).
- [x] Task 3: Verify Installation (AC: 2)
    - [x] Create a temporary script to attempt `import MiniRAG` (or the actual package name found in the repo).
    - [x] Confirm the import succeeds without errors.

## Dev Notes
- **Source Tree Location**: `src/MiniRAG/` is the designated location for the cloned repository. [Source: docs/architecture.md#Source Tree]
- **External Dependency**: MiniRAG (git-main). [Source: docs/architecture.md#Tech Stack]
- **Integration Approach**: The architecture specifies cloning MiniRAG into `src/MiniRAG`. Dependency management should successfully add requirements to `pyproject.toml` or `uv` environment. [Source: docs/architecture.md#Integration Approach]
- **Coding Standards**: Follow existing Python PEP8 conventions. [Source: docs/architecture.md#Coding Standards]

### Testing
- **Test File Location**: N/A for this setup task.
- **Test Standards**: Validation via import check is sufficient for this story.

## Change Log
| Date | Version | Description | Author |
| :--- | :--- | :--- | :--- |
| 2025-12-07 | 1.0 | Initial Draft | Bob (Scrum Master) |
| 2025-12-07 | 1.1 | Approved | Bob (Scrum Master) |

## Dev Agent Record
*To be populated by Dev Agent*

### Agent Model Used
Default Agent (James)

### Debug Log References
*To be populated by Dev Agent*

### Completion Notes List
1. Cloned MiniRAG to `src/MiniRAG` and removed `.git`.
2. Updated `pyproject.toml` to require Python `>=3.10, <3.14` and pinned project to Python 3.11 due to `numba` incompatibility with 3.14.
3. Installed dependencies from `src/MiniRAG/requirements.txt` via `uv add` (excluding `configparser`, `pipmaster`, `setuptools`).
4. Installed `sentence-transformers` manually as it was missing from requirements but needed for import.
5. Installed `minirag` in editable mode (`uv pip install -e src/MiniRAG`).
6. Verified import with `tests/verify_minirag_import.py`.

### File List
- `pyproject.toml`
- `.python-version`
- `uv.lock`
- `src/MiniRAG/`
- `tests/verify_minirag_import.py`

## QA Results
*To be populated by QA Agent*
